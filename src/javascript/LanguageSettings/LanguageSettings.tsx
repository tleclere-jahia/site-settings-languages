import React from "react";
import {LayoutContent, Typography} from "@jahia/moonstone"

export const LanguageSettings = () => <LayoutContent content={<Typography variant={'title'}>Hello world</Typography>}/>;
